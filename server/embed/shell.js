// html-artifacts editor shell — placeholder.
//
// Phase 3 replaces this file with the annotation editor (element picker,
// text-range selection, comment box, and "Send to agent"). For now it is a
// deliberate no-op so that /view/{id} can inject a stable, versioned script URL
// today and gain the editor later with no server change.
(function () {
  "use strict";
  if (window.console && console.debug) {
    console.debug("[html-artifacts] editor shell placeholder loaded");
  }
})();
